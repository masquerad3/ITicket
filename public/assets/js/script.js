document.addEventListener('DOMContentLoaded', () => {
  const form = document.getElementById('loginForm');
  if (!form) return;

  // Keep ONLY UI behavior here (toggle password, etc).
  // Do NOT prevent form submission.
});